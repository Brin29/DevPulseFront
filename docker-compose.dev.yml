services:
  frontend:
    image: node:22-alpine
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: devpulsefront_dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped