import { BrowserRouter } from "react-router-dom";
import AppContent from "./AppContent";

const AppRouter = () => (
    <BrowserRouter>
      <AppContent />
    </BrowserRouter>
);

export default AppRouter;
